\documentclass{article}
\usepackage{pstricks}
%Compile with latex, then dvips <name> -E to get an encapsulated PS image (still with .ps extension)
%can be included into a latex file using \includegraphics

%uncomment the next line to disable grids on figures for final draft.
%\let\psgrid\relax
\psset{gridcolor=green, subgridcolor=yellow}

\begin{document}
\thispagestyle{empty}

\begin{pspicture}(0,0)(5,3) %frame it
\psgrid

%Enter it here. 

\end{pspicture}


\end{document}
